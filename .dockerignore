.git
.gitignore
.env
.env.local
.env.*.local

# Build artifacts
.next
out
build

# Dependencies (reinstalled in the Docker build)
node_modules

# Dev / editor files
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# Service account key files — never bake credentials into the image
*.json.key
*-credentials.json
service-account*.json

# Test files
__tests__
*.test.ts
*.test.tsx
coverage

# Docker files themselves (no need to copy into the image)
Dockerfile
docker-compose*.yml
.dockerignore
